<%= form_with(model: fb_message, local: true) do |form| %>
  <% if fb_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fb_message.errors.count, "error") %> prohibited this fb_message from being saved:</h2>

      <ul>
        <% fb_message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :received %>
    <%= form.text_field :received %>
  </div>

  <div class="field">
    <%= form.label :reply %>
    <%= form.text_field :reply %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
